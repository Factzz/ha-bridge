# 🏠 HA Bridge (Home Assistant Integration)

**HA Bridge** คือแอปพลิเคชันเสริมสำหรับ **Rosemary OS** ที่ออกแบบมาเพื่อผู้ใช้งาน Smart Home โดยเฉพาะ ช่วยให้คุณสามารถนำข้อมูลของต้นไม้ไปแสดงผลและควบคุมผ่าน **Home Assistant** ได้อย่างง่ายดายโดยไม่ต้องแก้ไขโค้ดที่บอร์ด (No Firmware Flash Required)

---

## ✨ ฟีเจอร์หลัก (Features)

* 📊 **Real-time Sensors:** ดึงค่าความชื้น (Moisture), อุณหภูมิ (Temperature) และ VPD ไปสร้างเป็นกราฟใน Home Assistant ได้ทันที
* 💧 **Watering Control:** สั่งเปิดระบบรดน้ำผ่าน Home Assistant Dashboard ได้โดยตรง
* ⚙️ **Auto YAML Generator:** ระบบจะตรวจจับ IP ของเครื่องและสร้างโค้ดสำหรับ `configuration.yaml` ให้คุณคัดลอกไปวางได้เลย หมดปัญหาเรื่องพิมพ์ผิด
* ⚡ **Zero Config:** ทำงานผ่าน RESTful API ที่ฝังอยู่ใน Rosemary OS รุ่น 4.2 ขึ้นไป

---

## 🚀 วิธีการใช้งาน (How to setup)

1.  **ติดตั้งแอป HA Bridge:** เปิดหน้า Store ในแอป Rosemary ของคุณและกดติดตั้ง `HA Bridge`
2.  **เปิดแอปเพื่อรับโค้ด:** กดเปิดแอป HA Bridge ระบบจะคำนวณและแสดงโค้ด YAML ที่มี IP ของเครื่องคุณฝังอยู่แล้ว
3.  **คัดลอกโค้ด:** กดปุ่ม `COPY` เพื่อคัดลอกโค้ดทั้งหมด
4.  **ตั้งค่าใน Home Assistant:**
    * ไปที่ File editor หรือ VS Code ภายใน Home Assistant
    * เปิดไฟล์ `configuration.yaml`
    * วางโค้ดที่คัดลอกมาลงไป (โปรดระวังเรื่องการเว้นวรรค / Indentation)
5.  **รีสตาร์ทระบบ:** ไปที่ Developer Tools > กด Check Configuration > หากไม่มี Error ให้กด **Restart** Home Assistant

---

## 📝 ตัวอย่างโค้ดที่ระบบสร้างให้ (Example YAML)

```yaml
rest:
  - resource: [http://192.168.](http://192.168.)X.X/api/data
    scan_interval: 10
    sensor:
      - name: "Rosemary Moisture Zone 1"
        value_template: "{{ value_json.plants[0].moisture }}"
        unit_of_measurement: "%"
        icon: mdi:water-percent
      - name: "Rosemary Temp"
        value_template: "{{ value_json.env.temp }}"
        unit_of_measurement: "°C"
        device_class: temperature
      - name: "Rosemary VPD"
        value_template: "{{ value_json.env.vpd }}"
        unit_of_measurement: "kPa"

rest_command:
  rosemary_water_z1:
    url: "[http://192.168.](http://192.168.)X.X/api/water"
    method: POST
    payload: '{"index": 0}'
    content_type: 'application/json'
